import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
	console.log('Congratulations, your extension "indenter" is now active!');

	// We will implement more advanced indentation/alignment logic here if needed.
}

export function deactivate() {}
